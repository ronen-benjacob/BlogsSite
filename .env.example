# Flask Application Configuration
# Copy this file to .env and fill in your actual values

# Flask Secret Key (required for session management)
# Generate a secure random key for production
FLASK_KEY=your-secret-key-here

# Database Configuration
# For local development with SQLite (default if not set):
# Leave SQLALCHEMY_DATABASE_URI unset or set to: sqlite:///blog.db

# For production/deployment with PostgreSQL:
# Set SQLALCHEMY_DATABASE_URI to your PostgreSQL connection string
# Format: postgresql://username:password@hostname:port/database
# Example: postgresql://blog_user:blog_password_123@localhost:5432/blog_db
# For Docker Compose: postgresql://blog_user:blog_password_123@db:5432/blog_db
SQLALCHEMY_DATABASE_URI=postgresql://blog_user:blog_password_123@localhost:5432/blog_db

# Test Configuration (only used when running tests)
# Set to 'true' to run tests with PostgreSQL instead of in-memory SQLite
# Default: 'false' (uses SQLite in-memory for faster tests)
TEST_WITH_POSTGRES=false

# Optional: Custom test database URI (only used if TEST_WITH_POSTGRES=true)
# If not set, defaults to: postgresql://blog_user:blog_password_123@localhost:5432/blog_test_db
# TEST_DATABASE_URI=postgresql://blog_user:blog_password_123@localhost:5432/blog_test_db
